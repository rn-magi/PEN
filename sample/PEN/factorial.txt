整数 seisuu;
整数 i;
整数 fact;
出力 ("階乗を求めるプログラムです。\n");
出力 ("整数を入力してください。\n");
seisuu <- 入力;
もし ( seisuu > 0 ) ならば {
	fact <- seisuu;
	i <- seisuu - 1;
	条件( i > 1 )が成立している間以下を繰返す{
		fact <- fact * i;
		i <- i - 1;
	}
	出力 (seisuu , "の階乗は " , fact , " です。\n");
}さもなければ{
	出力 ("不正な数値が入力されました。\n");
}

