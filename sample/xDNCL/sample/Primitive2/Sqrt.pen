実数 hi,lo,x,y
整数 i
i ← input()
hi ← i
lo ← 1

繰り返し，
  | x ← (hi+lo)/2
  | y ← x*x-i
  | もし y > 0 ならば
  |   | hi ← x
  | を実行し，そうでなければ
  |   | lo ← x
  | を実行する
を，abs(y) < 0.000001 になるまで実行する

x を表示する
sqrt(i)を表示する
