/*   pr05-11.pen        (問題05-11)
 *   5個の数の入力を受付け，それらのうちの最大値
 *   と最小値を求めて出力するプログラム．
 */

整数 a
整数 max
整数 min
整数 i
a ← input()
max ← a
min ← a
i を 1 から 4 まで 1 ずつ増やしながら，
  | a ← input()
  | もし a > max ならば
  |   | max ← a
  | を実行する
  | もし a < min ならば
  |   | min ← a
  | を実行する
を繰り返す
maxと「が大きく」とminと「が小さい」を印刷する
