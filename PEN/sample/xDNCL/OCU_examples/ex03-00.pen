/*   ex03-00.pen                (例題03)
 *   2つの値の入力を受け付け，大きい方の値を変数maxに，
 *   小さい方の値を変数minに入れ，それらを出力するプログラム．
 */
整数 a
整数 b
整数 max
整数 min
a ← input()
b ← input()
もし a>b ならば
  | max ← a
  | min ← b
を実行し，そうでなければ
  | max ← b
  | min ← a
を実行する
max と「が大きく、」と min と「が小さい」を印刷する
