/* 三訂版 情報B P50-P51 （第一学習者） */
/* 平方根を求めるアルゴリズム          */

整数 n
実数 tate, yoko

「数を入力してください：」を改行なしで表示する
n ← input()
tate ← n
yoko ← 1

abs(tate - yoko) > 0.000001 の間，
  | yoko ← 0.5 * (yoko + tate)
  | tate ← n / yoko
を繰り返す

n と「の平方根は」と tate と「です」を表示する