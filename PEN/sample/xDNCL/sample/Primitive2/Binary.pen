/* 10進数 → 2進数 変換プログラム */

整数 i, x, y[32]

「10進数を入力：」を改行なしで表示する
x ← input()

i ← 1
x > 0 の間，
 | y[i] ← x % 2
 | x ← x / 2
 | i ← i + 1
を繰り返す

「2進数では 」を改行なしで表示する

i ← i - 1
i > 0 の間，
 | y[i] を改行なしで表示する
 | i ← i - 1
を繰り返す

「です．」を出力する

