/* 3つの入力値の最大/最小を表示するプログラム */

整数 a, b, c, max, min

a ← input()
b ← input()
c ← input()

もし a>b ならば
  | max ← a
  | min ← b
を実行し，そうでなければ
  | max ← b
  | min ← a
を実行する
もし c>max ならば
  | max ← c
を実行し，そうでなければ
  | もし c<min ならば
  |   | min ← c
  | を実行する
を実行する

maxと「が大きく」とminと「が小さい」 を表示する