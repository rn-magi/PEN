整数 sum, start, end

「入力値1～入力値2までを加算するプログラムです」を表示する

「入力値1：」を改行なしで表示する
start ← input()

「入力値2：」を改行なしで表示する
end ← input()

sum ← call(start, end)
「合計は」と sum と 「です」を出力する


関数 整数 call(整数 x,整数 y)
  | 整数 i, sum, temp
  | 
  | もし x > y ならば
  |   | temp ← x
  |   | x ← y
  |   | y ← temp
  | を実行する
  | 
  | i を x から y まで 1 ずつ増やしながら，
  |   | sum ← sum + i
  | を繰り返す
  | sum を返す
関数終了
