/* 綺麗な線分を描画するプログラム */

整数 x

/* 横200、縦200のwindowを開く */
gOpenWindow(200, 200)

x を 0 から 200 まで 1 ずつ増やしながら，
  | /* 線の色を設定する */
  | gSetLineColor(x, 0, 200-x)
  | 
  | /* 線を描画する */
  | gDrawLine(100-x/4, x, x, 100+x/4)
を繰り返す