/* スマイルマークを描画するプログラム */

整数 n

/* 横250、縦250のwindowを開く */
gOpenWindow(250, 250)

/* 線と塗りつぶしの色を黄色に設定 */
gSetLineColor(255, 255, 0)
gSetFillColor(255, 255, 0)

/* 丸い円を描画する */
gFillOval(0, 0, 250, 250)

/* 線と塗りつぶしの色を白色に設定 */
gSetLineColor(255, 255, 255)
gSetFillColor(255, 255, 255)

/* 両目を描画する */
gFillOval(60, 70, 20, 50)
gFillOval(170, 70, 20, 50)

/* 口にあたる部分を描画する */
n を 125 から 140 まで 1 ずつ増やしながら，
  | gDrawArc(60, n, 130, 70, 180, 180, 0 )
を繰り返す
